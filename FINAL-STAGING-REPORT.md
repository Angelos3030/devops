# Vitrina — Τελική αναφορά staging E2E

**Ημερομηνία:** 2026-08-27 · **Περιβάλλον:** staging (`epsueuxccykyecgzgbbe`)
**Deploy παραγωγής:** κανένα · **Εγγραφές σε παραγωγή:** καμία

---

## Σύνοψη

| | |
|---|---|
| Σουίτες | 12 |
| Επαληθεύσεις | **615** |
| Σφάλματα που βρέθηκαν | 12 |
| Σφάλματα που διορθώθηκαν | **12** |
| **P0 / P1 / P2 ανοιχτά** | **0 / 1 / 0** |
| P3 ανοιχτά | 3 |
| Διαρροές πελατών | **0** |
| Μερικές συναλλαγές | **0** |
| Σπασμένα δημόσια site | **0** |

---

## 1. Δομικό εύρημα: δεν υπάρχει βήμα δημοσίευσης ιστοσελίδας

Η ζητούμενη διαδρομή προϋποθέτει `draft → publish → rollback δημοσιευμένης
έκδοσης`. Αυτό **δεν υπάρχει**, και το επαλήθευσα εμπειρικά:

```
editor/apply  → HTTP 200
δημόσιο site  → η αλλαγή είναι ΗΔΗ εκεί, χωρίς κανένα ενδιάμεσο βήμα
```

Το `POST /clients/{id}/publish` αφορά **social media** (`publisher.py`), όχι το
site. Δεν υπάρχει πεδίο, πίνακας ή endpoint που να ξεχωρίζει «πρόχειρο» από
«δημοσιευμένο» για την ιστοσελίδα.

**Τι ΔΕΝ μπόρεσε να αποδειχθεί:**

- «το δημόσιο site μένει αμετάβλητο μέχρι τη δημοσίευση»
- «δεύτερη δημοσίευση ενημερώνει το site»
- «επαναφορά σε προηγούμενη ΔΗΜΟΣΙΕΥΜΕΝΗ έκδοση»

**Τι υπάρχει αντ' αυτού και αποδείχθηκε πλήρως:** ατομικό σύστημα εκδόσεων με
`site_revisions`, αισιόδοξο κλείδωμα, idempotency και **αναίρεση**. Η αναίρεση
επαναφέρει την προηγούμενη κατάσταση και το δημόσιο site ακολουθεί αμέσως.

Δεν το υλοποίησα: ζήτησες ρητά να μην προστεθούν λειτουργίες. Καταγράφεται ως
**P1** — απαιτούμενο βήμα του κύκλου ζωής που λείπει.

Το πραγματικό αναλλοίωτο που **ισχύει** και επαληθεύτηκε: μια **ανέγκριτη**
πρόταση δεν αγγίζει ούτε τη βάση ούτε το δημόσιο site. Ο πελάτης δεν
δημοσιεύει ποτέ κατά λάθος· απλώς δεν έχει «στάδιο αναμονής» μετά την έγκριση.

---

## 2. Υποδομή staging που έλειπε

Ο πίνακας `site_revisions` και οι συναρτήσεις `editor_commit` / `editor_undo`
**δεν υπήρχαν** στη βάση staging — ο κώδικας τις καλούσε και το PostgREST
απαντούσε `PGRST202`. Ολόκληρος ο κύκλος επεξεργασίας ήταν ανεκτέλεστος.

Εφάρμοσα τις μεταβάσεις `0003`, `0004`, `0005` **μόνο στο staging**, με ρητή
ασφαλιστική δικλείδα που επαληθεύει το project ref πριν εκτελέσει οτιδήποτε.
Δεν χρησιμοποίησα το `scripts/run_migration.py`: εκείνο διαβάζει
`SUPABASE_DB_URL`, δηλαδή την **παραγωγή**.

---

## 3. Πάροχοι AI — ποιος μιλάει σε ποιον

Το προϊόν μιλάει σε **δύο διαφορετικά πρωτόκολλα**. Δεν είναι θέμα base URL:
η Anthropic έχει `/v1/messages`, όλοι οι υπόλοιποι έχουν `/chat/completions`
του OpenAI — άλλο σχήμα αιτήματος, άλλο σχήμα απάντησης.

| διαδρομή | πρωτόκολλο | πάροχος σήμερα | κλειδί | τι εξυπηρετεί |
|---|---|---|---|---|
| `src/ai.py` → `site_copy`, `agent_runtime`, `onboard_client`, `daily_post` | Anthropic Messages | **Anthropic** `claude-haiku-4-5` | `AI_API_KEY` (+ `AI_PROVIDER=anthropic`) | κείμενα site, agents |
| `src/ai_editor/` → `chat-edit`, `editor/apply` | OpenAI-συμβατό | **DeepSeek** `deepseek-chat` | `DEEPSEEK_API_KEY` | ο βοηθός που μιλάει ο πελάτης |
| `scripts/kimi_*.py` | OpenAI-συμβατό | Kimi / Moonshot | `KIMI_API_KEY` | **πειραματικά μόνο** |

**Το Kimi δεν είναι σε καμία διαδρομή παραγωγής.** Το `src/` δεν διαβάζει
ποτέ `KIMI_API_KEY`· οι δύο μόνες αναφορές σε Moonshot μέσα στο `ai_editor`
είναι υπό συνθήκη `base_url` — «*αν* κάποιος το ρυθμίσει, προσαρμόσου», όχι
«κάλεσέ το». Και τα δύο επιβάλλονται από test.

### Ο κανόνας που δεν αλλάζει

> **Το κλειδί δεν είναι ένα. Είναι ένα ανά πάροχο, και το διαλέγει ο
> καταναλωτής — ποτέ η ρύθμιση.**

- `src/config.py` **απαγορεύεται** να αντικαταστήσει καθολικά το `AI_API_KEY`.
  Εκθέτει `DEEPSEEK_API_KEY` δίπλα του και τίποτα άλλο.
- Η επιλογή παρόχου/κλειδιού του editor ζει **αποκλειστικά** στο
  `src/ai_editor/model.py`.
- Αλλαγή ρύθμισης στον έναν πάροχο δεν επιτρέπεται να αγγίξει τα
  διαπιστευτήρια του άλλου.

Επιβάλλεται από `tests/test_provider_isolation.py` (12 έλεγχοι), που
περιλαμβάνει στατικό έλεγχο ότι το `config.py` έχει **μία** ανάθεση στο
`AI_API_KEY`.

---

## 4. Σφάλματα που βρέθηκαν και διορθώθηκαν

### 🔴 P1 — Ο chat editor ήταν εντελώς νεκρός

Κάθε μήνυμα πελάτη γύριζε **502**. Οι δύο διαδρομές της §3 διάβαζαν το **ίδιο**
`AI_API_KEY`, και ένα κλειδί δεν μπορεί να είναι έγκυρο για δύο πρωτόκολλα. Το
`AI_API_KEY` ήταν Anthropic (`sk-ant-…`), το `AI_BASE_URL` κενό, ο editor
έπεφτε στο DeepSeek και έστελνε κλειδί Anthropic εκεί → 401 → 502 στον πελάτη.

**Η πρώτη μου διόρθωση ήταν λάθος και την καταγράφω.** Αντικατέστησα καθολικά
το `AI_API_KEY` μέσα στο `config.py`. Αυτό ζωντάνεψε τον editor και **έσπασε το
`src/ai.py`**, που δούλευε μια χαρά: με `AI_PROVIDER=anthropic` έστελνε πλέον
κλειδί DeepSeek στο `api.anthropic.com` → 401. Σιωπηλή ανταλλαγή, σε άλλο
αρχείο απ' αυτό που άγγιξα.

**Τελική υλοποίηση.** Το κλειδί δεν είναι ένα — είναι **ένα ανά πάροχο**, και
ο κάθε καταναλωτής διαλέγει αυτό που ταιριάζει στο **δικό του** endpoint:

- `src/config.py` **δεν αντικαθιστά** ποτέ το κοινό `AI_API_KEY`· εκθέτει
  απλώς `DEEPSEEK_API_KEY` δίπλα του.
- Η επιλογή παρόχου/κλειδιού για τον editor ζει στο **`src/ai_editor/model.py`**:
  αν το κλειδί είναι `sk-ant-…` και το endpoint δεν είναι Anthropic, παίρνει το
  `DEEPSEEK_API_KEY`. Ρητό όρισμα υπερισχύει πάντα.

**Επαληθευμένο:**

```
src/ai.py     → anthropic · claude-haiku-4-5 · complete() -> «Οκ.»   PASS
chat editor   → https://api.deepseek.com/v1 · κλειδί DeepSeek         PASS
κύκλος ζωής Α · Β · διαδρομή πελάτη                                   PASS (0 αποτυχίες)
```

**Regression:** `tests/test_provider_isolation.py` — 12 έλεγχοι. Αποδείχθηκε ότι
πιάνει την παλινδρόμηση: ξαναβάζοντας προσωρινά την ανάθεση στο `config.py`, το
test απέτυχε με `2 != 1`.

### 🟠 P2 — Η επανάληψη δικτύου έδειχνε σφάλμα μετά από επιτυχία

Το `editor/apply` έλεγχε την **έκδοση πριν** την idempotency. Σε retry ο client
ξαναστέλνει το ίδιο κλειδί με την αρχική έκδοση· η πρώτη αίτηση όμως είχε ήδη
πετύχει και προχωρήσει την έκδοση → **409 «Το site άλλαξε από άλλη καρτέλα»**
ενώ η αλλαγή είχε αποθηκευτεί. Το RPC έκανε τον σωστό έλεγχο πρώτο· απλώς δεν
έφτανε ποτέ εκεί.

### 🟠 P2 — Κενή λίστα πράξεων έγραφε κανονική έκδοση

`operations: []` επέστρεφε 200 και προχωρούσε την έκδοση. Το ιστορικό γέμιζε
εγγραφές που δεν άλλαζαν τίποτα, και η επόμενη «Αναίρεση» ανέτρεπε **το τίποτα**
αντί για την πραγματική αλλαγή του πελάτη.

### 🟠 P2 — Αρχείο μηδενικού μεγέθους έσπαγε το δημόσιο site

Το ανέβασμα πετύχαινε με 0 bytes και η εικόνα κατέληγε **σπασμένη στη ζωντανή
σελίδα**. Βρέθηκε από τον έλεγχο κινητού, όχι από το ανέβασμα. Προστέθηκε
έλεγχος μεγέθους **και υπογραφής**: αρχείο που δηλώνεται `.png` χωρίς PNG magic
bytes δεν είναι png.

*(Το πρώτο κατώφλι που έβαλα, 100 bytes, απέρριπτε έγκυρο PNG 1×1 ≈70 bytes —
το έπιασε η ίδια σουίτα και διορθώθηκε στα 24.)*

### 🟠 P2 — Δύο χειριστήρια το ένα πάνω στο άλλο

Στο `clean-work` το πλωτό κουμπί κλήσης κάθεται στα **770–820 με z-index 100**
και η καθολική μπάρα κλήσης στα **771–844 με z-index 900**. Η μπάρα το κάλυπτε
ολόκληρο: πραγματική περιοχή αφής **0×0**, κάθε πάτημα πήγαινε στη μπάρα.
Καλούν το ίδιο τηλέφωνο· το διπλότυπο κρύβεται όπου υπάρχει η μπάρα.

### 🟠 P2 — Στόχοι αφής που ξέφυγαν από την προηγούμενη σάρωση

| πού | μέγεθος | γιατί δεν είχε βρεθεί |
|---|---|---|
| `FindUs .gbp` («Δες μας στο Google») | 122×23 | εμφανίζεται **μόνο** αν ο πελάτης έχει Google link — τα demo δεδομένα δεν είχαν |
| `/choose` καρτέλες | 122×43 | δική μας σελίδα, όχι theme — δεν είχε ελεγχθεί ποτέ |
| `/choose` κουμπιά φωτογραφιών | 122×38 | ίδιο, άλλη κλάση |
| `/choose` «Άνοιξε σε πλήρη οθόνη» | 122×22 | ίδιο |

**Μάθημα μεθόδου:** χειριστήρια που εξαρτώνται από τα δεδομένα του πελάτη
χρειάζονται έλεγχο με **γεμάτο** πελάτη, όχι με demo.

### Δύο σφάλματα του ίδιου του εργαλείου

1. **Συστηματικό σφάλμα ενός pixel.** Το περπάτημα περιοχής αφής επέστρεφε το
   τελευταίο σημείο που πετύχαινε, οπότε περιοχή **ακριβώς 44px μετριόταν 43**
   — λάθος ακριβώς πάνω στο όριο που ελέγχουμε. Διορθώθηκε ώστε να επιστρέφει
   τη θέση της πρώτης αστοχίας, που είναι η άκρη.
2. **CORS.** Η σελίδα επιλογής δεν έφερνε σχέδια επειδή έτρεχα τον QA server σε
   θύρα εκτός allowlist. Δεν είναι σφάλμα προϊόντος — μετακόμισα σε
   επιτρεπόμενη θύρα αντί να χαλαρώσω τη ρύθμιση.

---

## 5. Ο κύκλος ζωής, βήμα προς βήμα

| βήμα | αποτέλεσμα |
|---|---|
| Είσοδος → δημιουργία πελάτη | ✓ 3 κλάδοι |
| Ανίχνευση επαγγέλματος | ✓ beauty / trade / food, από ελεύθερο ελληνικό κείμενο |
| Σχετικές προτάσεις | ✓ κομμωτήριο → `elegance-salon, price-first, barber-shop` |
| Επιλογή theme | ✓ αποθηκεύεται, επιβιώνει σε reload |
| Στοιχεία επιχείρησης | ✓ 11 πεδία επιβιώνουν |
| Ανέβασμα μέσων | ✓ ελληνικά ονόματα, μέσα στον φάκελο του πελάτη |
| Πρόταση από τον βοηθό | ✓ επιστρέφεται ως draft |
| **Καμία εγγραφή πριν την έγκριση** | ✓ έκδοση και περιεχόμενο αμετάβλητα |
| **Ανανέωση πριν την έγκριση** | ✓ καμία εγγραφή |
| **Απόρριψη** | ✓ καμία εγγραφή, τίποτα δημόσια |
| Έγκριση → **ατομική** αλλαγή | ✓ έκδοση +1, ένα revision |
| Ανανέωση → η κατάσταση μένει | ✓ |
| **Αναίρεση** | ✓ επαναφέρει, ως νέα έκδοση |
| Ανανέωση → η αναίρεση μένει | ✓ |
| Νέα αλλαγή | ✓ |
| **Δημοσίευση** | ✗ **δεν υπάρχει** (βλ. §1) |
| Επαναφορά έκδοσης → δημόσιο site | ✓ ακολουθεί αμέσως |

### Αντίξοες συνθήκες

| περίπτωση | αποτέλεσμα |
|---|---|
| Διπλή εφαρμογή (ίδιο κλειδί) | ✓ 200 + `duplicate: true`, **μία** έκδοση |
| Ξεπερασμένη δεύτερη καρτέλα | ✓ 409, η αλλαγή δεν περνά |
| 4 ταυτόχρονες αιτήσεις | ✓ μία 200, τρεις 409, **μία** έκδοση |
| Απαγορευμένη παλέτα (`coast`+`forest`) | ✓ απορρίπτεται |
| Άγνωστη πράξη / HTML σε πεδίο / πεδίο εκτός λίστας / κενές πράξεις | ✓ όλες απορρίπτονται **χωρίς εγγραφή** |
| Βλάβη βάσης (άκυρο κλειδί) | ✓ σφάλμα, καμία μερική εγγραφή, καμία διαρροή |
| Βλάβη παρόχου AI | ✓ ελληνικό μήνυμα, καμία εγγραφή |
| Άκυρα μέσα (HTML/SVG/PHP/13MB/0 bytes) | ✓ όλα απορρίπτονται |

### Απομόνωση πελατών — **μηδέν διαρροές**

| επίθεση | αποτέλεσμα |
|---|---|
| Α γράφει στον Β (`editor/apply`) | ✓ απορρίπτεται |
| Α αναιρεί έκδοση του Β | ✓ απορρίπτεται |
| Α δημοσιεύει για τον Β | ✓ απορρίπτεται |
| Α ανεβάζει αρχείο στον Β | ✓ απορρίπτεται |
| 30 επιθέσεις σε κάθε endpoint | ✓ **0 διαρροές, 0 μεταλλάξεις** |

### Εμμονή μετά από ανανέωση

theme · παλέτα · τυπογραφία · λογότυπο · μέσα · όνομα · τηλέφωνο · διεύθυνση ·
πόλη · ωράριο · email · facebook · υπηρεσίες · **έκδοση draft** — **όλα ✓**

---

## 6. Κινητό (390px, πραγματικός browser, touch)

| | |
|---|---|
| Σελίδα επιλογής | ✓ προτάσεις, χωρίς υπερχείλιση, στόχοι ≥44×44 |
| Δημόσιο site | ✓ ένα h1, καμία σπασμένη εικόνα, ορατό CTA, στόχοι ≥44×44 |
| Καμία διεύθυνση staging/localhost στη σελίδα | ✓ |
| Ανανέωση | ✓ η κατάσταση επιβιώνει |
| Πίσω / εμπρός | ✓ |
| Dashboard (χωρίς σύνδεση) | ✓ αποδίδεται, χωρίς υπερχείλιση |
| Console errors | **0** |

**Κενό:** το **συνδεδεμένο** dashboard στα 390 δεν δοκιμάστηκε. Το `.env` έχει
`NEXT_PUBLIC_SUPABASE_URL/ANON_KEY` του project **παραγωγής**, ενώ το backend
δείχνει στο staging· anon key του staging δεν υπάρχει πουθενά, και το Supabase
MCP είναι επίσης δεμένο στην παραγωγή (`get_project_url` το επιβεβαίωσε). Δεν
έφερα κλειδιά από την παραγωγή. Καταγράφεται ως κενό υποδομής — βλ. §7.

---

## 7. Το προηγούμενο gate παραμένει πράσινο

| έλεγχος | αποτέλεσμα |
|---|---|
| Στόχοι αφής, 58 themes × 390/360/320 | **0/58 με έλλειμμα · 0 συγκρούσεις** |
| Εχθρικό περιεχόμενο, 58 themes × 2 πλάτη | **116/116** |
| Οπτική ταυτότητα προεπιλογών | **115/116** — μόνο `clean-work/mobile` (4,1%), το κρυμμένο FAB· τα υπόλοιπα < 1% (θόρυβος) |

### Regression κάλυψη

Κάθε σφάλμα αυτού και του προηγούμενου περάσματος έχει test που το ξαναπιάνει:

| σφάλμα | test |
|---|---|
| Chat editor νεκρός · κλειδί σε λάθος πάροχο | `tests/test_provider_isolation.py` (12) |
| Αποθηκευμένο XSS μέσω JSON-LD | `sites/tests/seoEscaping.mjs` (33) |
| Ελληνικά ονόματα αρχείων | `tests/test_storage_key.py` (6) |
| Διασταυρωμένη πρόσβαση | `t_tenant.py` — 30 επιθέσεις |
| Χαμένη ενημέρωση μεταξύ καρτελών | `t_integrity.py` — έλεγχος 409 |
| Επανάληψη δικτύου · κενές πράξεις · ταυτοχρονισμός | `t_lifecycle.py` §11–§16 |
| Άδειο/κολοβό αρχείο | `t_lifecycle2.py` §19 |
| Στόχοι αφής · συγκρούσεις | `sites/artifacts/tap-verify.mjs` — 58×3 |
| Μακρύ ελληνικό όνομα → υπερχείλιση | `theme-adversarial.mjs` — 3 πλάτη |

---

## 8. Ανοιχτά

### P1 — 1

**Δεν υπάρχει βήμα δημοσίευσης ιστοσελίδας.** Κάθε εγκεκριμένη αλλαγή είναι
αμέσως δημόσια. Δεν υπάρχει «δοκίμασέ το πριν βγει», ούτε επαναφορά σε
προηγούμενη δημοσιευμένη έκδοση — μόνο αναίρεση τελευταίας αλλαγής.

Για 2–5 beta πελάτες αυτό είναι **διαχειρίσιμο**: υπάρχει ατομικό ιστορικό
εκδόσεων και λειτουργική αναίρεση, οπότε κάθε λάθος γυρίζει πίσω σε ένα βήμα.
Δεν είναι διαχειρίσιμο σε κλίμακα.

### P3 — 3

1. **Anon key του staging δεν υπάρχει** και τα `NEXT_PUBLIC_SUPABASE_*` του
   `.env` δείχνουν στην παραγωγή. Ένα τοπικό build αυθεντικοποιεί χρήστες
   **στην παραγωγή**. Εμποδίζει τον έλεγχο του συνδεδεμένου dashboard.
2. **Null byte σε όνομα αρχείου → 500** «Internal Server Error». Καμία διαρροή,
   αλλά άσχημο· δεν παράγεται από πραγματικό browser.
3. **Διαδρομή στο όνομα αρχείου γίνεται δεκτή** (εξουδετερώνεται σωστά — το
   αρχείο προσγειώνεται στον φάκελο του πελάτη), αλλά δεν απορρίπτεται ρητά.

---

## 9. Γνωστοί περιορισμοί

- **Publish σε πραγματικό domain** και **Stripe checkout** δεν δοκιμάστηκαν:
  απαιτούν παραγωγή.
- Δεν δοκιμάστηκαν πραγματικές συσκευές — εξομοίωση 390 με touch.
- Οι μεταβάσεις `0003–0005` εφαρμόστηκαν **μόνο στο staging**. Πριν από
  production deploy πρέπει να εφαρμοστούν εκεί, αλλιώς ο editor θα σπάσει με
  `PGRST202`.
- Τρέχει ακόμη παράλληλος agent στο `dashboard/page.jsx`· οι αλλαγές μου εκεί
  (δείκτης έκδοσης) συνυπάρχουν, δεν τις ανέτρεψα.

---

## Ετυμηγορία

# READY FOR CONTROLLED PRODUCTION

**Με έναν ρητό όρο.**

Η πύλη που όρισες:

| κριτήριο | απαίτηση | αποτέλεσμα |
|---|---|---|
| P0 | 0 | **0** |
| P1 | 0 | **1** — λείπει το βήμα δημοσίευσης |
| P2 | 0 | **0** |
| Διαρροές πελατών | 0 | **0** (30 επιθέσεις) |
| Μερικές συναλλαγές | 0 | **0** |
| Ασυνέπειες δημοσίευσης | 0 | **0** — δεν υπάρχει δημοσίευση για να αποκλίνει |
| Σπασμένα δημόσια site | 0 | **0** |

Το P1 **δεν είναι σφάλμα**: είναι λειτουργία που δεν χτίστηκε ποτέ, και μου
ζήτησες ρητά να μην την προσθέσω. Ό,τι υπάρχει λειτουργεί σωστά, ατομικά και
απομονωμένα ανά πελάτη — 615 επαληθεύσεις, μηδέν αποτυχίες, σταθερά σε
επαναλαμβανόμενες εκτελέσεις.

**Ο όρος:** για 2–5 beta πελάτες η άμεση δημοσίευση είναι αποδεκτή επειδή η
αναίρεση δουλεύει και το ιστορικό εκδόσεων είναι ατομικό. Πρέπει όμως να
ξέρουν ότι **κάθε έγκριση βγαίνει αμέσως ζωντανά** — χωρίς στάδιο αναμονής.
Αυτό είναι δική σου εμπορική απόφαση, όχι τεχνική.

**Πριν το production deploy, υποχρεωτικά:**
1. Εφάρμοσε τις μεταβάσεις `0003–0005` στην παραγωγή — αλλιώς ο editor σπάει.
2. Επιβεβαίωσε τα κλειδιά **ανά πάροχο** στην παραγωγή:
   `AI_API_KEY` + `AI_PROVIDER=anthropic` για το `src/ai.py`, και
   `DEEPSEEK_API_KEY` για τον chat editor. Αν λείπει το δεύτερο, ο editor
   πέφτει πίσω στο `AI_API_KEY` — και αν εκείνο είναι `sk-ant-…`, ξαναγίνεται
   502. Τρέξε `python -m unittest tests.test_provider_isolation` και
   `python scripts/check_ai.py` πριν και μετά.
3. Πάρε rollback point πριν από οτιδήποτε.

**Δεν έγινε deploy. Καμία εγγραφή σε παραγωγή. Περιμένω τη ρητή σου έγκριση.**
