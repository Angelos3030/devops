{
  "theme_key": "frost-bakery",
  "component": "FrostBakery",
  "files": [
    {
      "path": "sites/lib/templates/FrostBakery.jsx",
      "content": "import s from './FrostBakery.module.css';\nimport Brand from './Brand';\nimport FindUs from './FindUs';\nimport SocialLinks from './SocialLinks';\n\nexport default function FrostBakery({ d }) {\n  const services = d.services || [];\n  const gallery = d.gallery || [];\n  const about = d.about || [];\n  const hours = d.hours || [];\n\n  const seasonNames = ['Spring', 'Summer', 'Autumn', 'Winter'];\n  const seasonIcons = ['🌸', '☀️', '🍂', '❄️'];\n\n  const visibleSeasons = gallery.slice(0, 4).map((item, i) => ({\n    item,\n    season: seasonNames[i],\n    label: `${seasonNames[i]} Collection`,\n    icon: seasonIcons[i]\n  }));\n\n  const findBuildService = services.find(s => s.title.toLowerCase().includes('build')) || services.find(s => s.price) || null;\n\n  const renderFlavorCard = (service, idx) => {\n    const swatchClass = s[`swatch${idx % 6}`] || s.swatch0;\n    return (\n      <div className={s.flavorCard} key={idx}>\n        <div className={`${s.flavorCardSwatch} ${swatchClass}`}></div>\n        <div className={s.flavorCardInfo}>\n          {service.price ? (\n            <span className={s.flavorCardType}>From {service.price}</span>\n          ) : service.duration ? (\n            <span className={s.flavorCardType}>{service.duration}</span>\n          ) : null}\n          <h3 className={s.flavorCardName}>{service.title}</h3>\n          <p className={s.flavorCardDesc}>{service.desc}</p>\n        </div>\n      </div>\n    );\n  };\n\n  return (\n    <div className={s.root}>\n      {/* Hidden checkbox for mobile nav toggle */}\n      <input\n        type=\"checkbox\"\n        id=\"frost-nav-toggle\"\n        className={s.navToggle}\n        aria-label=\"Toggle navigation\"\n        defaultChecked={false}\n      />\n      <label htmlFor=\"frost-nav-toggle\" className={s.hamburger} aria-hidden=\"true\">\n        <span className={s.hamburgerLine}></span>\n        <span className={s.hamburgerLine}></span>\n        <span className={s.hamburgerLine}></span>\n      </label>\n      <div className={s.sidebarOverlay}></div>\n\n      <aside className={s.sidebar} role=\"navigation\" aria-label=\"Main navigation\">\n        <div className={s.sidebarBrand}>\n          <Brand d={